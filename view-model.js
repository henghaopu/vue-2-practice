var app = new Vue({
    
    el: '#root',
    
    data: data,
    
    // any custom methods that we add should be nested within the methods object
    methods: {
        
        onClick: addName
        
    }
    
});